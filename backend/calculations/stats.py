from script import *



# призовые места
def get_user_main_stats(user_id):
    output = list()

    get_data("users", "user_id, team_id")
    get_data("teams", "...")


    first_place = 1


    return []user_id, first_place, second_place, ...]


def get_user_chart(user_id):

    #как правильно отправлять данные для графиков в каком формате?
    return [
        {
            "timestamp": # data
            "...": 1
        },
        {
        ...
        }
    ]